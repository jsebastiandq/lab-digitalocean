extends layout

block content
  div(class="min-h-screen flex items-center justify-center")
    div(class="bg-white p-8 rounded shadow-lg max-w-md w-full")
      h1(class="text-3xl font-bold mb-4 text-blue-600") Hello World!
      p(class="text-gray-600 mb-6") This is a Node.js + Tailwind CSS + Pug application.
      button(class="px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600 transition") Click Me
